services:
  - type: web
    name: social-proof-cards
    runtime: static
    staticPublishPath: .
    buildCommand: "npm ci && npm run build"
    headers:
      - path: /*
        name: Cache-Control
        value: public, max-age=3600
